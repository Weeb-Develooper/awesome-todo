import 'package:flutter/material.dart';

class Categories {
  final Color color;
  final Icon icon;
  final String name;
  final int itemCount;

  Categories(this.color, this.icon, this.name, this.itemCount);
}